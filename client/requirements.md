## Packages
framer-motion | Essential for smooth, high-quality scroll reveals and gaming-style micro-interactions.
react-hook-form | Form state management.
@hookform/resolvers | Zod validation integration for forms.

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
Tailwind Config - extend colors:
Add primary, secondary, neon accents to colors if needed, but CSS variables will handle most of it.
